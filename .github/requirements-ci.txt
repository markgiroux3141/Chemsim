# The versions the suite's pins were last measured with, on the user's machine.
# CI installs exactly these so a red run is a change in this repo, not a new
# RDKit canonicaliser or a new ruff rule. Bump them deliberately, in their own
# commit, and expect pins to move.
numpy==2.4.0
scipy==1.16.3
rdkit==2026.3.5
thermo==0.6.1
chemicals==1.5.2
fluids==1.3.1
matplotlib==3.10.8
pytest==9.0.2
pytest-xdist==3.8.0
ruff==0.15.2
