# NEXT — overwritten 2026-09-13

Overwritten whole each session: what is still true is re-typed, the rest is gone.

## State of the box

Every number came from a command run on 2026-09-13. The command is named.

| fact | value | command |
|---|---|---|
| tests | 1,345 collected, 1,342 -> 1,345: T23's three | `python -m pytest --co -q` |
| full suite | 1,345 passed in 30m09s, exit 0, AFTER today's change | `python -m pytest -q` |
| fast check | `./check.ps1`, ~90 s, green | ruff + docs + catalog + templates + silent + 62 smoke tests |
| expensive checks owed | NONE. All five `ok`; `suite` and `tolerance` both run today | `python tools/cadence.py` |
| templates | 59 rows, all `tier=family`, covering 46 catalog classes -- unmoved | `python tools/build_templates.py --check` |
| catalog | 1,583 compounds, 173 routes, 377 steps -- unmoved | `python tools/catalog.py` |
| routes template-ready / species-ready / both | 46 / 90 / 40 -- unmoved, and see below for why | `data/catalog/COVERAGE_REPORT.md` |
| routes runnable / playable | 47 runnable, 23 playable -- regenerated byte-identical | `data/catalog/PLAYABLE.md` |
| the shelf | 72 rows, 43 natural / 25 intermediate / 4 bottle | `data/catalog/shelf.psv` |
| pKa table | 37 `AcidPair` rows over 8 classes, 33 -> 37 | `python validation/pka_domains.py` panel 0 |
| ions derived from it | 38, was 34, and all four new ones are ANIONS | `tests/test_protonation.py` |
| shelf ions the six ion rows cannot price | 35, was 39, and they are ONE compound | `python validation/pka_domains.py` panel 2 (5 s) |
| corpus ions that want a pKa | 208 amine / 138 carboxylic / 102 phenoxide, behind 27 / 20 / 20 routes | same command, panel 3 |
| why templates are silent | 16 no-substrate, 8 needs-more-than-a-pair, 0 cannot-fire; `pool_species` 289 -> 290 | `python tools/classify_silent.py` (31 s) |
| `SAVE_VERSION` | 9 | `src/chemsim/engine/world.py:122` |
| line endings | mixed: `BACKLOG.md`, `NEXT.md`, `CHANGELOG.md`, the PSVs and `electrolyte.py` are CRLF; `validation/` and most tests are LF | `git ls-files --eol <file>` |

## Last session, in five lines

T23's shelf half. Four curated pKa rows -- malonic acid's two protons,
4-nitrophenol, coniferyl alcohol -- taken from PubChem's `iupacpka` collection,
which carries the determination AND its temperature series, so one paper gave
both the pKa and a van't Hoff `dH_diss`. Panel 2 fell 39 -> 35 and the
remainder is one compound, printed as a derived bound. Every scoreboard stayed
exactly where it was, which is the result rather than a disappointment: the
audit scores catalog compounds and the catalog names its ions one row at a
time. The suite was run twice and the tolerance audit once; nothing moved.

## Do this now

1. **T23's corpus half -- pKa rows in route-demand order.** Spec in
   `BACKLOG.md`. `python validation/pka_domains.py` panel 3 is the ranking:
   salicylic acid is named by 4 routes, vanillin by 3, indigo by 3, bisphenol-a
   and gallic acid by 2 each. Query PubChem `iupacpka` by CID (the recipe is in
   `_PAIRS`' new comments) and prefer a determination that carries a
   temperature series. Never mix two compilations inside one trend, and never
   write a pKa you cannot source. When `tests/test_protonation.py`'s
   `len(ions)` assertion fails, that is its PROMPT firing: re-measure
   `validation/catalog_coverage.py` and record what did or did not move.
   *Done when:* panel 3's "want a pKa" count has fallen, each new row names its
   source, and `./check.ps1` is green.

2. **T16 - the other loose sulfur-dioxide slot.** Spec in `BACKLOG.md`.
   `sulfur_dioxide_oxidation_by_nitrogen_dioxide` still writes SO2 as
   `[O:1]=[S:2]=[O:3]`, which matches a sulfate, and two shelf rows are
   sulfates. Three slots rather than eight, so it is wasted work per flask and
   not the 16-minute bomb T12 defused. Tighten it to `[OX1]=[SX2]=[OX1]` as T12
   did for the Claus row. Chain 2's carrier step, so Ea/A must not move; the
   tolerance audit is clean as of today, which makes this a good session to
   spend it on.
   *Done when:* the slot matches one shelf species and
   `tests/test_lead_chamber.py` is green with the same numbers.

3. **T26 - three phenol rows claim `dH_diss = 0` and it is not zero.** Found
   in T23 and spec'd in `BACKLOG.md`. The field cannot distinguish "measured
   small" from "never looked up", and phenol and eugenol are the second kind
   while malonic acid is honestly the first.
   *Done when:* no `dH_diss` of 0.0 stands for an unmeasured quantity, and the
   tolerance audit is re-run because equilibria above 298 K move.

## Decisions already taken — do not reopen

- **Tannic acid's 35 shelf ions are a bound, not rows.** Four of them hang off a
  parent the table can price and 31 off a parent that is itself an unpriced
  microspecies, so writing the four does not close the 35. Panel 2 derives this
  rather than asserting it. T14's rule in a second place: a table closes a list
  and never a generator.
- **A class-wide pKa rule for phenols is refused**, and T23's own rows made the
  refusal harder rather than easier: 3 of the 10 curated pairs are now ordered
  BACKWARDS by the substituent sum, and one of the three (coniferyl alcohol
  under phenol) is not explained by salicylate's hydrogen bond. Full argument
  and what a rule would need first: `docs/design/phenol-pka-rule-refused.md`.
  The amine class is refused more simply: three rows spanning 6.04 units.
- **A missing ion has two gaps and only one is a pKa.** An ion whose neutral
  parent has no thermochemistry is not unblocked by curating an acidity; that
  is the whole mineral-oxyacid class (89 ions, 0 wanting a pKa) and it is T25.
- **A curated pKa moves the SHELF instrument, not the coverage scoreboard.**
  `COVERAGE_REPORT.md` scores the 1,583 catalog compounds and the catalog names
  its ions one row at a time; `pka_domains.py` panel 2 is where these rows show.
  Quote the right instrument rather than concluding the rows bought nothing.
- **No species `build_network` registers may be unpriceable**; **a pKa the
  engine derived is not one it measured, and the player sees which**; **the
  plateau value is derived from `_PAIRS`, never typed**; **a route is credited
  with every step product**; **backwards is retrosynthesis**; **the expensive
  checks are clocked in commits**; **the headline is templates fired**.

## Open questions for the user

- **`rxnmapper` as a curation-time dependency** for T2, build-time only as
  `chemicals` is; without it T2 needs an RDKit-only mapper.
- **`hydrogen-sulfide` is still a shelf row at tier `intermediate`** while the
  closure and a pyrrhotite flask both make it, and no catalog route does.
- **Network access is available and PubChem answers**, which is how T23's four
  values were sourced. Say if a build-time-only rule should apply to it.

## Do not

- Do not hand-edit `COVERAGE_REPORT.md`, `PLAYABLE.md`, `ROUTE_INDEX.md`,
  `*_data.py`, `reachable.psv`, `species_roles.psv` or `silent_templates.psv`.
- Do not write a pKa you cannot source, do not put an estimator in front of one,
  and do not re-open the tannic acid bound or the phenol refusal.
- Do not read `docs/history/` whole (grep it) and do not add a physics module.
- Do not stamp a cadence row you did not run, clear a red one, or rewrite a
  mixed-ending file whole -- read and write bytes.
