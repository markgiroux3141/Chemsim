# NEXT — overwritten 2026-09-13

Overwritten whole each session: what is still true is re-typed, the rest is gone.

## State of the box

Every number came from a command run on 2026-09-13. The command is named.

| fact | value | command |
|---|---|---|
| tests | 1,345 collected -- unmoved; T27 edited two pins and added none | `python -m pytest --co -q` |
| fast check | `./check.ps1`, ~90 s, green | ruff + docs + catalog + templates + silent + 62 smoke tests |
| ion-table tests | 188 passed over the ten test files that touch `_PAIRS` | `pytest tests/test_protonation.py tests/test_pka_domains.py ...` |
| expensive checks owed | none today, but `reachable` goes due on the NEXT commit (10 of 10) | `python tools/cadence.py` |
| templates | 59 rows, all `tier=family`, 46 catalog classes -- unmoved | `python tools/build_templates.py --check` |
| catalog | 1,583 compounds, 173 routes, 377 steps -- unmoved | `python tools/catalog.py` |
| routes template-ready / species-ready / both | 46 / 90 / 40 -- byte-identical | `python validation/catalog_coverage.py` |
| routes runnable / playable | 47 / 23 -- regenerated byte-identical | `python tools/build_playable.py` |
| the shelf | 72 rows, 43 natural / 25 intermediate / 4 bottle | `data/catalog/shelf.psv` |
| pKa table | 41 `AcidPair` rows over 8 classes, 37 -> 41 | `python validation/pka_domains.py` panel 0 |
| ions derived from it | 42, was 38, and one of the four is the FIFTH cation | `tests/test_protonation.py` |
| corpus ions wanting a pKa | 444, was 448: 207 amine / 136 carboxylic / 101 phenoxide | same command, panel 3 |
| routes behind them | 48, was 51, over 27 / 17 / 19 routes by class | same command, panel 3 |
| shelf ions the table cannot price | 35, unmoved, and they are ONE compound | same command, panel 2 |
| why templates are silent | 16 no-substrate, 8 needs-more-than-a-pair, 0 cannot-fire; `pool_species` 290 | `python tools/classify_silent.py` (31 s) |
| `SAVE_VERSION` | 9 | `src/chemsim/engine/world.py:122` |
| line endings | mixed: `BACKLOG.md`, `NEXT.md`, `CHANGELOG.md`, the PSVs and `electrolyte.py` are CRLF; `validation/` and most tests are LF | `git ls-files --eol <file>` |

## Last session, in five lines

T27 took T23's corpus half and worked the route-demand ranking down. Four rows:
vanillin, adipic acid's second proton with its first re-sourced onto the same
paper, cinnamic acid, dimethylammonium. Panel 3 fell 448 -> 444 ions and 51 ->
48 routes, and `adipic-acid-route`, `perkin-route` and `vanillin-lignin` now
have no pKa-shaped hole at all. The top of the ranking turned out to be refusals
rather than rows, and they are written down under T23 in `BACKLOG.md`: salicylic
acid wants a microspecies nobody measures, gallic acid's pKa1 spans 1.3 units at
one ionic strength, four more have no rows in the collection. Coverage did not
move, for the T23 reason. No slow check was owed, and none was run.

## Do this now

1. **T23's ranking, further down.** Spec in `BACKLOG.md`, which now carries the
   refusals so they are not re-derived. `python validation/pka_domains.py`
   panel 3 is the live ranking. Recipe: PubChem `iupacpka` by CID, prefer a
   determination carrying a temperature series, take both protons of a diacid
   from one paper. Resolve a name to a CID by cross-checking the molecular
   formula against the catalog SMILES -- neither key alone is safe. Do not
   write a pKa you cannot source, and do not mint a `dH_diss` of 0.0 for a
   phenol or an amine; that is T26, and it already blocks two ready rows.
   *Done when:* panel 3's count has fallen, each new row names its source, and
   `./check.ps1` is green.

2. **`reachable` is due.** 35 minutes, so it is the user's call, but it went due
   on T27's commit and nothing has re-derived `derived/reachable.psv` since
   2026-09-12. `python tools/build_reachable.py`, then `python tools/cadence.py
   --record reachable --result pass|fail --note "..."`. T27 added four ions the
   network can now register, so the pool could move -- `classify_silent.py`
   regenerating byte-identical is no evidence it did not, because it reads the
   stale artefact.
   *Done when:* `reachable.psv` is regenerated and the ledger row is recorded.

3. **T26 - a `dH_diss` of 0.0 cannot say "unmeasured", and it blocks two rows.**
   Spec in `BACKLOG.md`. Widened by T27: the amine class has the same hole as
   the phenols, and dimethylammonium's sourced +50.0 beside ammonium's +52.2
   measures how large a claim a zero is there. Salicylaldehyde (8.37) and
   n,n-dimethylaniline (5.15) are sourced and waiting on it, 2 routes each.
   *Done when:* no `dH_diss` of 0.0 stands for an unmeasured quantity, those two
   rows are in, and the tolerance audit is re-run.

## Decisions already taken — do not reopen

- **The top of the pKa ranking is refused, not pending.** Salicylic acid (4
  routes) wants the phenol-first MICROSPECIES and no compilation measures one;
  both its macroscopic constants are already in the table. Gallic acid spans
  3.13 to 4.46 on pKa1 at the SAME ionic strength. Indigo, bisphenol-a,
  hydroxylamine and indoxyl have no rows in `iupacpka` at all. The list with its
  CIDs is under T23 in `BACKLOG.md`.
- **Both protons of a diacid come from one determination.** T27 moved adipic's
  first from an unsourced 4.43 to Howell and Fisher's 4.42 to hold this, and the
  0.01 costs 0.06 kJ/mol. The diacid curve now has both ends: malonic 2.85 units
  of separation at one CH2, adipic 0.99 at four.
- **An assumed abscissa is not a measured one.** The dimethylamine series' first
  point has a blank temperature field; the spacing says 0 C and the fit barely
  moves either way, and it is still dropped. +50.0 is the five stated points.
- **Tannic acid's 35 shelf ions are a bound, not rows**; **a class-wide pKa rule
  for phenols is refused** (`docs/design/phenol-pka-rule-refused.md`); **a
  missing ion has two gaps and only one is a pKa**; **a curated pKa moves the
  shelf and corpus ion instruments, never `COVERAGE_REPORT.md`**, which scores
  the 1,583 catalog compounds and names ions one row at a time.
- **No species `build_network` registers may be unpriceable**; **a pKa the
  engine derived is not one it measured, and the player sees which**; **the
  plateau value is derived from `_PAIRS`, never typed**; **a route is credited
  with every step product**; **backwards is retrosynthesis**; **the expensive
  checks are clocked in commits**; **the headline is templates fired**.

## Open questions for the user

- **`reachable` is 35 minutes and it is due.** Task 2 above. Say whether to
  spend it, or it will go on being deferred.
- **`rxnmapper` as a curation-time dependency** for T2, build-time only as
  `chemicals` is; without it T2 needs an RDKit-only mapper.
- **`hydrogen-sulfide` is still a shelf row at tier `intermediate`** while the
  closure and a pyrrhotite flask both make it, and no catalog route does.

## Do not

- Do not hand-edit `COVERAGE_REPORT.md`, `PLAYABLE.md`, `ROUTE_INDEX.md`,
  `*_data.py`, `reachable.psv`, `species_roles.psv` or `silent_templates.psv`.
- Do not write a pKa you cannot source, put an estimator in front of one, or
  re-open the tannic acid bound, the phenol refusal or T23's refusal list.
- Do not read `docs/history/` whole (grep it) and do not add a physics module.
- Do not stamp a cadence row you did not run, clear a red one, or rewrite a
  mixed-ending file whole -- read and write bytes.
