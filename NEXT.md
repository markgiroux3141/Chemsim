# NEXT — overwritten 2026-09-16

Overwritten whole each session: what is still true is re-typed, the rest is gone.

## State of the box

Every number came from a command run on 2026-09-16. T29 touched the shelf, the
playable audit and two test files; no template, price or trajectory moved, and
`COVERAGE_REPORT.md` and `silent_templates.psv` regenerated byte-identical.

| fact | value | command |
|---|---|---|
| **route coverage ceiling** | **110 template-ready, ~66 intersection. 173 is NOT the target** | the script in `docs/design/route-coverage-ceiling.md` |
| routes template-ready / species-ready / both | 66 / 95 / 53, and 50 / 95 / 43 on hand-typed rows alone | `python validation/catalog_coverage.py` |
| routes runnable / playable | 51 / 23 -- `PLAYABLE.md` scores the `family` tier ALONE, on purpose | `data/catalog/PLAYABLE.md` footer |
| fed but unrunnable / the ceiling it implies | 24 / 52 | same file, §8 and its last section |
| **granting the whole shelf work order** | **23 -> 51 playable, which is EVERY runnable route** (was 23 -> 48) | `tests/test_playable_levers.py` |
| corpus species no provider prices | 401 of 1,583 | `python validation/catalog_coverage.py` |
| templates | 126 rows: 68 `family` hand-typed, 58 `literal` extracted; 103 catalog classes | `python tools/build_templates.py --check` |
| reaction classes with a template | 116 of 240, 64 of them a family row | `python validation/catalog_coverage.py` |
| template rows against their catalog step | 96 pass, 16 partial, 9 wrong-product, 3 no-substrate, 2 no-class over 282 steps | `python tools/check_template_products.py` (4 s) |
| tests | **1,361 collected. The last KNOWN red test is green** -- T28's 7 and T29's survivor are all closed. The suite itself was last run at T32 (1 fail / 1,360) and has NOT been re-run | `python -m pytest -q`, ~30 min, ASK FIRST |
| fast check | `./check.ps1`, ~95 s, green, 88 smoke tests | ruff + docs + catalog + extracted + templates + template products + silent + smoke |
| catalog | 1,583 compounds, 173 routes, 377 steps | `python tools/catalog.py` |
| why templates are silent | 30: 20 no-substrate, 10 needs-more-than-a-pair, 0 cannot-fire | `python tools/classify_silent.py` (2 s) |
| the shelf sweep | 666 pairs, 23,140 reactions, 37 of 67 templates fire, 1,254 s. **STALE: the shelf grew by 4 species today and this was not re-run** | `derived/reachable.psv` footer |
| pKa table / ions derived | 41 `AcidPair` rows, 43 ions priced | unchanged since T28 |
| **the shelf** | **75 rows: 43 natural, 21 intermediate, 11 bottle** (T29 retiered 5, added 4) | `python tools/build_shelf.py`, or `len(SHELF)` |
| the rate ceiling | clean at 298 K; coldest crossing still 417 K | `python validation/rate_ceiling.py` (NOT re-run today) |
| `SAVE_VERSION` | 9 | `src/chemsim/engine/world.py:122` |
| expensive checks | **0 of 5 due.** `suite` stands RED from T32 but its named survivor is what T29 fixed; `tolerance` stands RED from T28 | `python tools/cadence.py` |
| line endings | mixed: `BACKLOG.md`, `NEXT.md`, `CHANGELOG.md`, `CLAUDE.md`, `tests/test_shelf.py`, the catalog PSVs and `physical_data.py` are CRLF; `check.ps1`, most of `tests/`, `tools/`, `docs/design/` are LF | `git ls-files --eol <file>` |

## Last session, in five lines

T29: `MADE_SOMEWHERE` took every step product raw, so a catalyst -- which this
corpus writes on BOTH sides of its step -- counted as something the corpus
MAKES. Ten `nickel` rows read as smelting nickel and `furfural-route` row 1
(`xylose + water -> xylose`) as hydrolysing its own pentose, so five unmakeable
species sat in the EARNABLE tier, three saying "nothing in the corpus smelts
this" in their own note while the tier column said otherwise. Behind it, tiers
were read off the per-ROUTE buckets, and a route can miss one species the corpus
makes and one it does not -- so `methane` and `acetic-anhydride` were filed under
neither and the work order was two rows short. It now reaches 51 playable, every
runnable route, against 48: the feedstock half of the board is closed.

## Do this now

1. **T33 -- `CHANGELOG.md` is at 459 lines, past the 400 its own header rolls
   at.** Nothing enforces it (`check_docs.py` caps an ENTRY, not the file), so it
   is a convention going stale rather than a red check. A whole-file move, CRLF,
   older half into `docs/history/changelog-2026-09.md`. Task 1 only because it
   is the one open item needing no measurement first.
   *Done when:* under 400 lines, the rolled half in one new `docs/history/`
   file, and `./check.ps1` green.

2. **T2d -- the promotion gate cannot see a wrong mapping.** Gates T2c, so it
   comes first. Verification compares canonical SMILES, the engine's own
   identity, so a symmetric product hides a mis-mapping: the Diels-Alder row
   joins the ring at the wrong pair of carbons and still writes cyclohexene.
   Re-run each row on ONE substituted analogue and report where it lands.
   *Done when:* the count of rows their own step does not constrain is
   measured, or the idea is refused in writing.

3. **T2c -- promote one literal row to `family`.** Still the cheap playable
   route: granting all 58 extracted rows would take runnable 51 -> 61. Check the
   mapping by hand (T2d), argue the barrier, move the row into `templates.psv`.
   *Done when:* one class moves tier and `PLAYABLE.md`'s headline moves with it.

## Decisions already taken — do not reopen

- **A species the corpus writes on both sides of a step is NOT made by it.**
  Now in all three instruments: `catalog.made_by`, `build_playable.needs` and
  `build_playable.MADE_SOMEWHERE`. A catalyst is bought, never earned.
- **The four catalyst metals are `bottle` rows -- the design call T29 was asked
  to make.** `nickel`, `cobalt`, `palladium`, `platinum` are on the shelf, but
  tiered as a purchase: no engine work makes them earnable. `NOT_NATURAL_NOTES`
  argued exactly this while the tier column contradicted it; `test_shelf.py`
  now pins the four by name.
- **A shelf tier is a question about a SPECIES, not its route** -- a route can
  want one the corpus makes and one it does not. `CHAIN_SPECIES`/`BOTTLE_SPECIES`.
- **A route may never be charged with its own target, and the rule is ORDER.**
  `bayer-process` and `contact-process` ARE primed with theirs, correctly.
- **`bp.RUNNABLE`/`bp.PLAYABLE` are the `family` path and `COVERAGE_REPORT.md`
  is every tier.** A test comparing a level must use `cc.FAMILY_TEMPLATE_CLASSES`.
- **A test that pins a LEVEL gets re-numbered; pin the relation too.**
  `test_shelf.py` said so in its own docstring and pinned three levels anyway.
- **A class grant is priced differently by each scoreboard, both right.**
- **A literal row is irreversible and declares no orders, no alpha, no rho.**
- **173 is not the target.** Quote 66. **Change the rate, not the queue** --
  per-row pKa curation is real work and is not task 1.
- **A pinned count is not a guard**; **a generated file COMPUTES a comparison
  rather than hard-coding it**; **no species `build_network` registers may be
  unpriceable**; **backwards is retrosynthesis**; **a class names a mechanism,
  never an outcome**; **never declare what detailed balance derives**.

## Open questions for the user

- **The full suite has not run since T32**, where the single failure was the
  test T29 just fixed. Its 6 tests are green in isolation, as are the 47 in the
  three files the shelf change could touch, so a wholly green suite is plausible
  for the first time -- but NOT measured. ~30 min, due at 4 commits (at 2).
- **`derived/reachable.psv` is stale**: the shelf grew 71 -> 75 rows, so the
  sweep's pool changed. Not due (3 of 10 commits), 35 min, your call.
- **`validation/tolerance_audit.py` has stood RED since T28**, same three
  pre-existing findings. Nothing since touched `numerics/`, `vessel/` or
  `network/`, so it is not owed -- but it has never once been green.

## Do not

- Do not hand-edit `literal.psv`, `needs_review.psv`, `COVERAGE_REPORT.md`,
  `PLAYABLE.md`, `ROUTE_INDEX.md`, `*_data.py`, `reachable.psv`,
  `species_roles.psv`, `silent_templates.psv` or `template_products.psv`.
- Do not chase 173, re-derive the ceiling, make per-row pKa curation task 1,
  write a pKa you cannot source, or reopen T2, T3, T28c, T29, T30, T31 or T32.
- Do not put a `literal` row in `templates.psv` or a `family` row in
  `literal.psv`, and do not edit `shelf.psv` against the audit -- the test
  prints the work order and the file follows it.
- Do not read `docs/history/` whole (grep it), add a physics module, stamp a
  cadence row you did not run, or rewrite a mixed-ending file whole.
